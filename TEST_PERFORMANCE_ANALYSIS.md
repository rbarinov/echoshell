# Test Performance Analysis

**Дата:** 2025-11-27

---

## ⏱️ Почему тесты работают долго?

### Причина: Большие задержки для синхронизации

**Проблема:** SessionStateManager - это singleton, который загружает состояние из UserDefaults при инициализации. Для правильной изоляции тестов пришлось добавить задержки.

### Текущие задержки:
- **500ms** (500_000_000 nanoseconds) - для критических операций
- **200-300ms** - для обычных операций  
- **100ms** - для быстрых проверок

### Время выполнения:
- **~37 секунд** на весь набор тестов
- **~0.7 секунды** на тест в среднем

### Почему так долго?

1. **Множественные задержки в каждом тесте:**
   - Очистка состояния: 200ms
   - Установка состояния: 500ms
   - Проверка состояния: 500ms
   - **Итого: ~1.2 секунды на тест**

2. **52 теста × 1.2 секунды = ~62 секунды** (теоретически)
   - На практике меньше из-за параллельного выполнения

3. **UserDefaults синхронизация:**
   - `UserDefaults.standard.synchronize()` - синхронная операция
   - Может занимать время на симуляторе

---

## 🔧 Как ускорить?

### Вариант 1: Уменьшить задержки (рисковано)
- 500ms → 300ms для большинства операций
- Может привести к нестабильности тестов

### Вариант 2: Использовать правильную синхронизацию
- Вместо задержек использовать `await` и правильные async/await паттерны
- Использовать `Task.yield()` вместо `Task.sleep()`

### Вариант 3: Dependency Injection для тестов
- Создать тестовую версию SessionStateManager
- Не использовать singleton в тестах

---

## 📊 Текущие результаты

- **Пройдено:** ~48 тестов
- **Упало:** ~8 тестов
- **Время:** ~37 секунд

---

## ✅ Рекомендации

1. **Оставить текущие задержки** - тесты стабильнее
2. **Оптимизировать позже** - после того как все тесты пройдут
3. **Использовать параллельное выполнение** - Xcode уже делает это

**Вывод:** Долгое выполнение - это компромисс между стабильностью и скоростью. Для CI/CD это приемлемо.
