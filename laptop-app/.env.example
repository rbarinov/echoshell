# =============================================================================
# EchoShell Laptop Application - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.sample .env
# =============================================================================

# -----------------------------------------------------------------------------
# Tunnel Configuration (Required)
# -----------------------------------------------------------------------------
# URL of the tunnel server (e.g., https://tunnel.example.com)
TUNNEL_SERVER_URL=https://tunnel.example.com

# API key for authenticating with the tunnel server during registration
# This must match the TUNNEL_REGISTRATION_API_KEY on the tunnel server
TUNNEL_REGISTRATION_API_KEY=your_tunnel_registration_api_key_here

# Authentication key for laptop-to-mobile communication
# Generate a secure key: openssl rand -hex 32
# This key is included in the QR code and must match what the iPhone app expects
LAPTOP_AUTH_KEY=your_secure_laptop_auth_key_here

# Optional: Custom name for this laptop (displayed in tunnel server)
# Default: "My Laptop"
LAPTOP_NAME=My Laptop

# -----------------------------------------------------------------------------
# AI Agent Configuration (Required)
# -----------------------------------------------------------------------------
# Provider for the AI agent: "openai" or "cerebras"
# Default: "openai"
AGENT_PROVIDER=openai

# API key for the AI agent provider
# For OpenAI: Your OpenAI API key (sk-...)
# For Cerebras: Your Cerebras API key
AGENT_API_KEY=your_agent_api_key_here

# Optional: Model name for the AI agent
# For OpenAI: e.g., "gpt-4o-mini", "gpt-4o", "gpt-4-turbo"
# For Cerebras: e.g., "gpt-oss-120b"
AGENT_MODEL_NAME=gpt-4o-mini

# Optional: Base URL for the AI agent API (for custom endpoints)
# For Cerebras: Required, e.g., "https://api.cerebras.ai/v1"
# For OpenAI: Usually not needed (uses default OpenAI endpoint)
AGENT_BASE_URL=

# Optional: Temperature for AI responses (0.0 to 2.0)
# Default: 0 (deterministic)
AGENT_TEMPERATURE=0

# -----------------------------------------------------------------------------
# Speech-to-Text (STT) Configuration (Required)
# -----------------------------------------------------------------------------
# STT provider: "openai" or "elevenlabs"
# Default: "openai"
STT_PROVIDER=openai

# API key for STT provider
# For OpenAI: Your OpenAI API key (sk-...)
# For ElevenLabs: Your ElevenLabs API key
STT_API_KEY=your_stt_api_key_here

# Optional: Base URL for STT API (for custom endpoints)
# For OpenAI: Usually not needed (uses default: https://api.openai.com/v1)
# For ElevenLabs: Usually not needed (uses default: https://api.elevenlabs.io/v1)
STT_BASE_URL=

# Optional: STT model name
# For OpenAI: "whisper-1" (default)
# For ElevenLabs: "eleven_multilingual_v2" (default)
STT_MODEL=whisper-1

# -----------------------------------------------------------------------------
# Text-to-Speech (TTS) Configuration (Required)
# -----------------------------------------------------------------------------
# TTS provider: "openai" or "elevenlabs"
# Default: "openai"
TTS_PROVIDER=openai

# API key for TTS provider
# For OpenAI: Your OpenAI API key (sk-...)
# For ElevenLabs: Your ElevenLabs API key
TTS_API_KEY=your_tts_api_key_here

# Optional: Base URL for TTS API (for custom endpoints)
# For OpenAI: Usually not needed (uses default: https://api.openai.com/v1)
# For ElevenLabs: Usually not needed (uses default: https://api.elevenlabs.io/v1)
TTS_BASE_URL=

# Optional: TTS model name
# For OpenAI: "tts-1" (faster, cheaper) or "tts-1-hd" (higher quality)
#   Default: "tts-1"
# For ElevenLabs: "eleven_multilingual_v2" (default)
TTS_MODEL=tts-1

# Optional: TTS voice name
# For OpenAI: "alloy", "echo", "fable", "onyx", "nova", "shimmer"
#   Default: "alloy"
# For ElevenLabs: Voice ID (e.g., "21m00Tcm4TlvDq8ikWAM")
TTS_VOICE=alloy

# -----------------------------------------------------------------------------
# Web Interface Configuration (Optional)
# -----------------------------------------------------------------------------
# Port for the localhost-only web interface
# Default: 8002
WEB_INTERFACE_PORT=8002

# -----------------------------------------------------------------------------
# Workspace Configuration (Optional)
# -----------------------------------------------------------------------------
# Root directory for workspaces (where workspaces and repositories are stored)
# Default: ~/work
WORK_ROOT_PATH=~/work

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------
# Explicit path to .env file (overrides default .env loading behavior)
# Useful for custom deployment scenarios
# Default: Not set (uses laptop-app/.env and root .env)
DOTENV_CONFIG_PATH=

# -----------------------------------------------------------------------------
# System Environment Variables (Used as Fallbacks)
# -----------------------------------------------------------------------------
# These are typically set by your system and don't need to be in .env:
# - HOME: User home directory (used as default working directory)
# - SHELL: Default shell (used for terminal sessions)
# - ZDOTDIR: Zsh configuration directory (used for zsh initialization)

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1. All API keys can use the same OpenAI key if using OpenAI for all services
# 2. The LAPTOP_AUTH_KEY should be a secure random string (use: openssl rand -hex 32)
# 3. The TUNNEL_REGISTRATION_API_KEY must match the tunnel server's configuration
# 4. For production, ensure all sensitive values are kept secure and not committed to git
# 5. The .env file is automatically ignored by git (.gitignore)
