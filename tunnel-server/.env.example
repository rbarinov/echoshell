# Tunnel Server Environment Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=3000
HOST=127.0.0.1
NODE_ENV=production

# Public Host Configuration
# For production with nginx + Let's Encrypt (SSL offloading):
PUBLIC_HOST=example.com:443
PUBLIC_PROTOCOL=https

# For development:
# PUBLIC_HOST=localhost
# PUBLIC_PROTOCOL=http

# Security
# Generate a strong random key securely:
#   openssl rand -hex 32        (256-bit hex, recommended)
#   openssl rand -base64 32     (256-bit base64, alternative)
# Never use weak or predictable keys in production!
# Use different keys for each environment (dev/staging/production)
TUNNEL_REGISTRATION_API_KEY=your-secret-api-key-here

# Configuration Notes:
# - PORT: Internal port tunnel server listens on (must match nginx proxy_pass)
# - HOST: Bind address (127.0.0.1 for localhost-only, 0.0.0.0 for all interfaces)
# - PUBLIC_HOST: Your public domain with port (e.g., domain.com:443)
# - PUBLIC_PROTOCOL: https when using SSL/TLS (nginx handles SSL termination)
# - TUNNEL_REGISTRATION_API_KEY: Secret key for tunnel registration (generate securely above)
